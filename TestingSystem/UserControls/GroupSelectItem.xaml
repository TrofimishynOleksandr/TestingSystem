<UserControl x:Class="TestingSystem.UserControls.GroupSelectItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestingSystem.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="15" d:DesignWidth="100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="textBox" Grid.Column="0" HorizontalAlignment="Stretch" Margin="1" MinWidth="100" Text="{Binding Text}" FontSize="14"/>
        <CheckBox x:Name="checkBox" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" IsChecked="{Binding IsSelected}"/>
    </Grid>
</UserControl>
