<UserControl x:Class="TestingSystem.UserControls.FinalAnswer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestingSystem.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="360">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="textBox" Grid.Column="0" Width="310" HorizontalAlignment="Left" Margin="0 10 10 10" Text="{Binding Text}" FontSize="14"/>
        <CheckBox x:Name="checkBox" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" IsEnabled="False" IsChecked="{Binding IsRight}"/>
    </Grid>
</UserControl>
